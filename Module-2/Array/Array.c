// Array is use to store the multiple value inside the single variable.
// Array's starting index is '0'.
#include<stdio.h>
int main()
{
    // Define the array
    int array[5] = {1,5,8,9,6};
    printf("\nValue of the array[2]: %d", array[2]);
    return 0;
}