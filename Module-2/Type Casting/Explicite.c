// Explicits type conversion is use to convert the one data type to other data types
// Which was done by user forcefully
#include<stdio.h>
int main()
{
    int a = 45, b = 4;
    // int ans; 
    // ans = (float)a / (float)b;
    printf("\nDivision of the a and b is: %f", (float)a / (float)b);
    return 0;
}