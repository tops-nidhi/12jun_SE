#include<stdio.h>
void main()
{
    // int no = 49;
    int no;
    // char nm;
    // printf("Enter your name: ");
    // scanf("%c", &nm);
    printf("Enter the value of no: "); //use to pass the messages
    scanf("%d",&no); //To take the value from the user
    printf("Your roll no is: %d",no);
    // printf("\nYour name is: %c", nm);
}

/*
    static value:-
        -which was define during the sourcecode creation
    Dynamic value:-
        - Which wan define during  hte programme execution(run)
*/